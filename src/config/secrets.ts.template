/**
 * Local secrets template
 *
 * Copy this file to secrets.ts and fill in your values:
 *   cp src/config/secrets.ts.template src/config/secrets.ts
 *
 * secrets.ts is gitignored and should never be committed.
 */

// Supabase credentials (from your Supabase project settings > API)
export const SUPABASE_URL = 'YOUR_SUPABASE_URL';
export const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';

// HotUpdater OTA endpoint
export const HOT_UPDATER_URL = 'YOUR_HOT_UPDATER_URL';

// Separate sync IDs for dev (simulator) vs prod (real device)
// Generate UUIDs at: https://www.uuidgenerator.net/
export const SYNC_USER_ID_DEV = 'YOUR_DEV_UUID';
export const SYNC_USER_ID_PROD = 'YOUR_PROD_UUID';
